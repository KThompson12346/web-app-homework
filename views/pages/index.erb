<h1>Welcome to U-Tube the number one video sharing site</h1>
<h3>Newly uploaded videos</h3>
  <% @posts.each do |post| %>
    <div>
      <h3><a href="<%= post[:id] %>"><%= post[:title] %></a></h3>
      <p><%= post[:post_body] %></p>
    </div>
  <% end %>
